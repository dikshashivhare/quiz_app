<p id="notice"><%= notice %></p>
<div class="container">
<div class="card">
    <h4 class="card-header text-center card-title"><%= @subject.name %></h4>
  <div class="card-body">
    <h5 class="subject-headings">Subject Description:</h5>
    <p class="card-text job-values"><%= @subject.description %></p>
    <%= link_to 'Edit', edit_admin_subject_path(@subject), class: "btn btn-primary" %> 
    <%= link_to 'Back', admin_subjects_path, class: "btn btn-primary" %>
  </div>
  </div>
</div>

