<div class="container">
  <div class="row">
      <div class="col-md-10 offset-md-1">
    <div class="card mt-4">
      <div class="card-header">
          <h1 class="card-title text-center">Editing subject</h1>
     </div>
      <div class="card-body">
          <%= render 'form', subject: @subject %>
          <%= link_to 'Show', admin_subject_path {@subject}, class: "btn btn-primary mt-2" %>
          <%= link_to 'Back', admin_subjects_path, class: "btn btn-primary mt-2" %>
      </div>
    </div>
  </div>
</div>
